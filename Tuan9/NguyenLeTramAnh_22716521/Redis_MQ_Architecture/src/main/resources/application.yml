server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/system_arch_db
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update #
    show-sql: true

  # C?u h�nh Redis
  data:
    redis:
      host: localhost
      port: 6379
